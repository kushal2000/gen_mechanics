"""Evolution-loop drivers: run a population forward, record what it becomes.

Separate from the grammar itself. ``hand_sampler.mutate`` answers "which hands
are one step away"; this answers "where does a population of them end up".
"""
